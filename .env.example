# OpenAI — optional for MVP. Without a key, coach/essay APIs use high-quality demo responses.
OPENAI_API_KEY=

# Supabase — optional for MVP. UI runs fully on dummy data until these are set.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
